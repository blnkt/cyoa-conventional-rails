<% @chapters.each do |chapter| %>
  <div class="panel panel-body">
    <%= link_to chapter.prompt, adventure_chapter_path(@adventure, chapter), class: "btn btn-block btn-default" %>
  </div>
<% end %>